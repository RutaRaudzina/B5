package com.example.b5

class ButtonObject {
    var nextFragmentId: Int = 0
    var fraction: Double = 0.00

    constructor(nextFragmentId: Int, fraction: Double){
        this.nextFragmentId = nextFragmentId
        this.fraction = fraction
    }
}